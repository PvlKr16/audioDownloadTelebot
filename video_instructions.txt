1. download and install python
2. install ffmpeg:
   in cmd: winget install ffmpeg
           ffmpeg -version
 3. install requirements:
        python -m pip install -U pip
        python -m pip install yt-dlp requests